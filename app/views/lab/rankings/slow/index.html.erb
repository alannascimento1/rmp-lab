<h1>Ranking sem cache</h1>
<div class="hint">
  Agregacao com JOIN e GROUP BY rodando do zero em toda request.
</div>

<table>
  <tr><th>autor</th><th>comentarios recebidos</th></tr>
  <% @rows.each do |name, total| %>
    <tr><td><%= name %></td><td><%= total %></td></tr>
  <% end %>
</table>
